export interface Profile {
  name: string;
  email: string;
  enterprise: string;
  password: string;
  confirmPassword: string;
  accepted: string;
}
export interface Auth {
  email: string;
  password: string;
}
